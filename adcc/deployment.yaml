apiVersion: apps/v1
kind: Deployment
metadata:
  name: adcc
  labels:
    app: adcc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: adcc
  template:
    metadata:
      labels:
        app: adcc
    spec:
      containers:
      - name: adcc
        image: kind-game:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: GAME_DURATION
          value: "30"
        ports:
        - containerPort: 443
        readinessProbe:
          httpGet: {path: /api/state, port: 443}
        livenessProbe:
          httpGet: {path: /api/state, port: 443}