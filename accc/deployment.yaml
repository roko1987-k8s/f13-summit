apiVersion: apps/v1
kind: Deployment
metadata:
  name: accc
  labels:
    app: accc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: accc
  template:
    metadata:
      labels:
        app: accc
    spec:
      containers:
      - name: accc
        image: kind-game:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: GAME_DURATION
          value: "30"
        ports:
        - containerPort: 443
        readinessProbe:
          httpGet: {path: /api/state, port: 443}
        livenessProbe:
          httpGet: {path: /api/state, port: 443}